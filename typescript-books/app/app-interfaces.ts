export interface modelInterface {
	searchQuery: string;
	searchLimit: number;
	searchOrder: string;
}

export interface headerItemsInterface {
	name: string;
	clickFunc: number;
	text: string;
	showBtn: Boolean;
}
