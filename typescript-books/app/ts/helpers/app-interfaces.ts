export interface modelInterface {
	searchQuery: string;
	searchLimit: number;
	searchOrder: string;
}

export interface headerInterface {
	name: string;
	clickFunc: number;
	text: string;
	showBtn: Boolean;
}
